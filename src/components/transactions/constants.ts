import { TransactionType } from '@core/index';

const NAME = 'transaction';
const PLURAL_NAME = 'transactions';

const TRANSACTION_TYPES = [
  TransactionType.TransferFund,
];

export {
  NAME, PLURAL_NAME,
  TRANSACTION_TYPES,
};
